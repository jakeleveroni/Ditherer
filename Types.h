#pragma once
namespace Core
{
    const enum RETURN_CODES
    {
        INVALID_FILE = -1,
        RESULT_OK = 0,
        NULL_PTR = 1,
    };

    const enum COLORS
    {
        WHITE = 255,
        BLACK = 0,
        THRESHOLD = 127
    };
}
